# VibeJoyBar v0.9.7: AI 生产力闭环 —— Push-to-Talk (按住说话) 语音协同研发方案

## 🎯 目标与范围
让用户达成 **100% 脱离键盘的终极 AI 对话姿势**：单手手持 Joy-Con 靠在椅背上，按住手柄肩键（如 ZL/L）即可直接说话输入，松开手柄肩键即自动停止录入并一键发送至 Codex / ChatGPT！

### 核心功能指标

#### 1. Push-to-Talk (PTT) 动作体系 (`vibejoy` 核心驱动)
- **DSL 动作定义**：
  - 新增 `ptt:dictation`（调用 macOS 原生系统听写）与 `ptt:hotkey:<keys>`（自定义录音热键）；
  - 支持参数配置：`auto_send: bool`（松开时是否自动发送 Enter）与 `settle_ms: int`（停录到发送的文字沉淀等待延迟，默认 300ms）；
- **按下/释放双态协同驱动**：
  - **按下 (Button Down)**：模拟触发系统听写唤醒快捷键（如连续触发 2 次 Fn 或快捷键组合），系统麦克风即刻聆听；
  - **按住 (Button Held)**：麦克风保持开启录音状态；
  - **松开 (Button Up)**：发送停止听写信号，等待文字完全落入输入框后，自动触发 `tap:enter`，行云流水完成会话提交。

#### 2. VibeJoyBar 控制面板与动作预设库
- **快捷动作库集成**：
  - 在 `MappingInspector` 推荐动作栏中加入「按住说话 · Push-to-Talk 语音输入」一键预设；
- **配置面板细节**：
  - 支持在设置中选择 macOS 系统听写的唤醒方式（默认快捷键、自定义组合键）；
  - 支持设置松开是否自动发送。
